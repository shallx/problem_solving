#include <iostream>
#include <vector>
#include <list>
#include <math.h>
#include <map>
#include <string.h>

#define sd(x) scanf("%d", &x);
#define sdd(x, y) scanf("%d %d", &x, &y);
#define ForC(cs, t) for (int cs = 1; cs <= t; cs++)

using namespace std;

int main(){
    int t,cs,n, W, H;
    string s;
    sd(t);
    ForC(cs,t){
        sdd(W,H);
        getline(cin, s);
        
    }
    return 0;
}